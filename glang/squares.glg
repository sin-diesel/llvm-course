// program to count squares of numbers from 1 to 30

int idx = 0;
int y[30] = 10;

int square(int a)
{
    return a * a;
}

int main()
{
    idx = 2;
    for idx from 0 to 30
    {
        y[idx] = square(idx);
    }
    y[1] = 1;
return 0;
}
